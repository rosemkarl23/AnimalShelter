package com.Java.Pointwest.beans;

public class HRPersonnel extends Employee {
	public void computeHRSalary() {
		if (hoursWorked == 0.0) {
			totalSalary = 0.0;
		} else {
			totalSalary = (salary * hoursWorked);
		}
	}
}